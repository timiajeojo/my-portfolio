---
import { portfolio } from "../data/portfolio";
---

<section
  id="home"
  class="hero relative isolate overflow-hidden"
>

  <div class="container-custom relative z-10 flex min-h-screen items-center py-32">

    <div class="max-w-5xl">

      <!-- Availability -->

      <div
        class="hero-enter"
      >
        <span class="availability-badge">

          <span class="availability-dot"></span>

          {portfolio.availability}

        </span>
      </div>

      <!-- Greeting -->

      <p
        class="hero-enter delay-1 mt-12 text-lg font-medium text-zinc-400"
      >
        Hello, I'm
      </p>

      <!-- Name -->

      <h1
        class="hero-enter delay-2 mt-4 text-6xl font-black tracking-tight sm:text-7xl lg:text-8xl xl:text-9xl"
      >
        <span class="hero-name">
          {portfolio.name}
        </span>
      </h1>

      <!-- Role -->

      <div
        class="hero-enter delay-2 mt-8 flex min-h-[48px] items-center"
      >

        <span
          id="typed-role"
          class="text-2xl font-semibold text-zinc-300 md:text-4xl"
        >
          {portfolio.role}
        </span>

      </div>

      <!-- Description -->

      <p
        class="hero-enter delay-3 mt-10 max-w-2xl text-lg leading-9 text-zinc-400 md:text-xl"
      >
        {portfolio.hero.description}
      </p>

      <!-- CTA Buttons -->

      <div
        class="hero-enter delay-4 mt-14 flex flex-wrap gap-4"
      >

        <a
          href="#projects"
          class="btn-primary magnetic"
        >
          View Projects
        </a>

        <a
          href="#contact"
          class="btn-secondary magnetic"
        >
          Contact Me
        </a>

        <a
          href={portfolio.resume}
          download
          class="btn-secondary magnetic"
        >
          Download Résumé
        </a>

      </div>

      <!-- Social Links -->

      <div
        class="hero-enter delay-4 mt-16 flex flex-wrap items-center gap-8"
      >

        <a
          href={portfolio.socials.github}
          target="_blank"
          rel="noopener noreferrer"
          class="hero-link"
        >
          GitHub
        </a>

        {
          portfolio.socials.linkedin && (
            <a
              href={portfolio.socials.linkedin}
              target="_blank"
              rel="noopener noreferrer"
              class="hero-link"
            >
              LinkedIn
            </a>
          )
        }

        {
          portfolio.socials.twitter && (
            <a
              href={portfolio.socials.twitter}
              target="_blank"
              rel="noopener noreferrer"
              class="hero-link"
            >
              X
            </a>
          )
        }

        <a
          href={`mailto:${portfolio.contact.email}`}
          class="hero-link"
        >
          Email
        </a>

      </div>

    </div>

  </div>

  <!-- Scroll Indicator -->

  <a
    href="#about"
    class="hero-scroll hero-enter delay-4"
    aria-label="Scroll to About section"
  >

    <span class="mouse">

      <span class="wheel"></span>

    </span>

    <span>
      Scroll
    </span>

  </a>

</section>